import numpy as np 
def f(x):
    return x**4+10*x**3+33*x**2+40*x-2
x=np.linspace(-5,5,1000)
y=f(x)
max_val=np.max(y)
min_val=np.min(y)

print("Наибольшее значение функции:", max_val)
print("Наименьшее значение функции:", min_val)
